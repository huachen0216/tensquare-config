server:
  port: 9010
spring:
  application:
    name: tensquare-friend #指定服务名
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tensquare_friend?chracterEncoding=utf-8&useSSL=true
    username: root
    password: 123456
  jpa:
    database: MySQL
    show-sql: true
  redis:
    host: 127.0.0.1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6868/eureka
  instance:
    prefer-ip-address: true #支持跨域访问
jwt:
  config:
    key: fathua
